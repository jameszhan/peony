<%- nginx.www_paths.each do|loc, alias_path| -%>
location <%= loc %> {
	charset                         utf-8;
	
	autoindex 						on;
	autoindex_localtime 			on;
	autoindex_exact_size 			off;
	
	alias							<%= alias_path %>;
}
<%- end -%>
